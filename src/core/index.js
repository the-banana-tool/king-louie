const { createCore, PROVIDER_LABELS } = require('./create-core');
const { DEFAULT_SETTINGS, mergeSettings } = require('./settings');
module.exports = { createCore, PROVIDER_LABELS, DEFAULT_SETTINGS, mergeSettings };
